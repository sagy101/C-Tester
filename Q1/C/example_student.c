#include <stdio.h>

int main() {
   // Example student C file.
   // This file (example_student.c) will be ignored by git.
   // The .gitkeep file in this directory ensures the C/ folder is tracked.
   printf("Hello World\n");
   return 0;
} 