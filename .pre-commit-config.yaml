repos:
  - repo: local
    hooks:
      - id: privacy-audit-staged
        name: Privacy audit staged files
        entry: python tools/privacy_audit.py --staged
        language: system
        pass_filenames: false
